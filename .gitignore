# segredos e config local (NUNCA commitar)
.env
.env.*
.netlify/
.claude/

# conteúdo proprietário: prompt do Método CORE + instância/closers do Cal.
# HOJE esse arquivo só tem dados de exemplo (placeholder) e está commitado
# de propósito, pra os builds via Git funcionarem e os cron jobs (agenda,
# keepalive-supabase) serem registrados de verdade pela Netlify. Quando
# tiver o conteúdo REAL (instância/closers), volte a ignorá-lo aqui e
# avise o Claude pra tirar do repo antes de commitar qualquer coisa nele.
# netlify/_private.mjs

# dependências e arquivos de sistema
node_modules/
.DS_Store
*.log

# material de referência (não faz parte do app)
referencia/
